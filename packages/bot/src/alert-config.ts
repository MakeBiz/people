// Пороги системного (отдельного) сигнала по отделу. Вынесено в конфиг (раздел 4 спеки):
// командный сигнал срабатывает, когда агрегат отдела падает при стабильных людях —
// это указывает на проблему в руководителе/процессах, а не в конкретных людях.
export const DEPT_SIGNAL = {
  // падение среднего UWES total отдела к прошлому замеру, при котором поднимаем сигнал
  engagementDrop: 1.0,
  // падение eNPS отдела (пунктов) к прошлому замеру (включается с кампаниями)
  enpsDrop: 15,
  // минимум человек в отделе, чтобы агрегат был осмысленным
  minPeople: 4,
};

// Сообщения системных сигналов (имя отдела подставит движок).
export const DEPT_SIGNAL_MESSAGE = {
  engagement_drop:
    "🟡 Системный сигнал по отделу «{dept}»: средняя вовлечённость упала {prev} → {current} при стабильных индивидуальных показателях. Стоит посмотреть на нагрузку, процессы и руководителя, а не на отдельных людей.",
};
